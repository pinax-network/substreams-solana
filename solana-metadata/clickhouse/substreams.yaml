specVersion: v0.1.0
package:
  name: solana_metadata_clickhouse
  version: v0.1.0
  url: https://github.com/pinax-network/substreams-solana
  description: SPL Token & Metaplex metadata events for Solana blockchains (ClickHouse).
  image: ../../image.png

imports:
  db: ../substreams.yaml

modules:
  - name: db_out
    use: db:db_out

sink:
  module: db_out
  type: sf.substreams.sink.sql.v1.Service
  config:
    schema: "./schema.sql"
    engine: clickhouse
    postgraphile_frontend:
      enabled: false

network: solana
